<img width="658" height="113" alt="zram_" src="https://github.com/user-attachments/assets/ad91fe95-0c98-43fc-bb76-303f607a466f" /> 

Разметка диска до создания swap файла. Как создавался файл подкачки через zram, выбор пал на zram тк мешала файловая система сделать это через базовые команды fallocate или dd. 
Первая команда sudo modprobe zram она загружает модуль ядра.
Вторая команда sudo zramctl -f -s 2048M, сама команда настраивает устройство zram, zramctl сама утилита -f (find) приказав системе искать свободное место; -s 2048М (макс место). 
Третья команда sudo mkswap /dev/zram0 форматирует созданное пространство как пространство swap. 
Четвертая команда sudo swapon /dev/zram0 активирует устройство как активный файл подкачки. wiki.archlinux.org/title/Zram.

<img width="714" height="116" alt="zram" src="https://github.com/user-attachments/assets/991490f2-b39d-4651-85b3-ebc19becf56d" /> 

Здесь видно, что zram появился и работает.
Переходим к созданию пользователей. User мы добавляем через команду useradd -m -s /bin/bash user. -m автоматом создает домашнюю папку, а -s назначает командную оболочку bash. 

<img width="651" height="178" alt="d3255aeec374ca37" src="https://github.com/user-attachments/assets/595c8791-9b50-4fe4-a736-3211af5865f3" /> 

Далее мы задаем пароли. Используем echo "user:user" | chpasswd (для рута делаем также только меняем имя и пароль соответственно). su - user команда чтобы переключиться с рута на юзера. 
su - переключает пользователя. userdel -r user этой командой мы можем удалить пользователя. 
Что произойдет с данными на диске при выборе автоматической разметки? lsblk через эту команду мы можем посмотреть какие диски у нас есть. 
Если мы выберем автоматическую разметку, то у нас старые данные будут полностью удалены. Стираются все разделы, далее создаются новые разделы под средством форматирования. 

(Работа аналитика системы Льва Денисенко)
Для анализа системы и понимания работает ли она в штатном режиме мы  должны собрать данные.
В первую очередь надо узнать ядро и архитектуру системы. Для этого используем команду name -a(флаг -a используется для вывода всей информации). Из вывода указанного на скриншоте ниже видно что ядром системы является Linux, названием компьютера в сети archiso, версия ядра 7.1.8-arch1-3, а так же другие подробности сборки ядра.
Далее с помощью команды cat мы читаем файл /etc/os-release в котором хранится информация о текущем дистрибутиве.
<img width="1170" height="491" alt="uname_osrelease" src="https://github.com/user-attachments/assets/a96b78b7-b4f8-40f0-a464-c10c5ddb786b" />

Следующей информацией которую мы получили с помощью команды uptime - это системное время, время работы, количество активных пользователей и нагрузку на систему.
<img width="824" height="87" alt="uptime" src="https://github.com/user-attachments/assets/398cc797-e0c9-4567-ae26-ca6edff97051" />

Так же можно получить имя пользователя с помощью команды whoami. Но можно использовать команду id и получить более полную информацию о пользователе, его id, id его группы и прочую информацию.
<img width="460" height="45" alt="id" src="https://github.com/user-attachments/assets/1aec5418-4afc-49c5-983c-4379295b0bba" />

Командой Isbik мы получаем информацию о всех накопителях(HDD/SSD и прочие).
df -h позволяет нам увидеть свободное место на дисках(благодаря флагу -h мы получим информацию в формате удобном для чтения, а не байтах и блоках). 
<img width="978" height="384" alt="df-h" src="https://github.com/user-attachments/assets/4ff7c11c-d18d-4547-94c0-45152bd831b7" />

Дальше для получения подобной информации но об оперативной памяти и файле подкачке используется команда free -h.
<img width="1052" height="133" alt="free_-h" src="https://github.com/user-attachments/assets/c26614e7-bbe4-4604-9d75-bfdf4a18188c" />

Для информации о cpu мы используем команду lscpu и для удобства используем дополнительную команду head -20 (lscpu|head -20) это ограничит вывод только первыми 20 строками.
<img width="1346" height="617" alt="lscpu_head_-20" src="https://github.com/user-attachments/assets/75506240-b4d4-4de0-bd2c-67acb025b485" />

При необходимости посмотреть текущие запущенные процессы используется команда ps aux и для ограничения количества строк  используется head.
<img width="1181" height="504" alt="ps_aux" src="https://github.com/user-attachments/assets/ee98f6fe-7224-456f-9749-ac4298b30565" />
И последней используемой нами командой была systemctl list-units --tupe=service --state=running это не самая простая команда так что лучше разобрать её части:
1) systemctl команда для управления systemd
2) list-unitsм показывает все загруженные в данный момент systemd-юниты.
3) --tupe=service фильтр на только службы
4) --state=running фильтр на активные службы
Команда необходима для проверки того что правда работает, а не все что активно.
<img width="1300" height="625" alt="systemstl" src="https://github.com/user-attachments/assets/02ffacb4-60f0-4feb-8366-866728aeec20" />

